public class FrameTesting {
    public static void main(String[] args) {
        /*
        JFrame frame = new JFrame(); //creates a new frame
        frame.setTitle("JFrame title goes here"); //sets the title of frame
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); //exit out of application
        frame.setResizable(false); //prevent frame from being resized
        frame.setSize(420,420); //sets the x and y dimensions
        frame.setVisible(true); //makes the frame visible

        ImageIcon image = new ImageIcon("horde.jpg"); //create an ImageIcon
        frame.setIconImage(image.getImage()); //change icon of frame
        frame.getContentPane().setBackground(new Color(0x99230A)); //change color of background
        */

        MyFrameForFrameTesting myFrame = new MyFrameForFrameTesting(); // the name is not necessary if you're not using it.


    }
}
